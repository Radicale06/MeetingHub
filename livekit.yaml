port: 7880
bind: 0.0.0.0

rtc:
  port_range_start: 7882
  port_range_end: 7892
  tcp_port: 7881
  # use_external_ip: true  # Uncomment in production when behind NAT

turn:
  enabled: true
  domain: localhost
  tls_port: 5349
  udp_port: 3478

webhook:
  urls:
    - http://host.docker.internal:6001/livekit/webhook

room:
  auto_create: true
  empty_timeout: 300  # 5 min after last participant leaves

logging:
  level: info
